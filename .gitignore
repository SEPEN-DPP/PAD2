node_modules/
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
.env
.env.*
.DS_Store
Thumbs.db

# Configuração local sensível — nunca commitar
src/config/firebaseConfig.local.js
.firebaserc

# Dados gerados com informação pessoal real (ex.: índice de busca da Relação
# de Advogados) — só existem publicados no Hosting, nunca no histórico do Git.
public/dados/

functions/node_modules/
functions/.env
