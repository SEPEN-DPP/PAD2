/**
 * Exemplo de contrato de template (não implementado). Fixa a assinatura que
 * todo módulo em src/templates deve seguir — ver README.md deste diretório.
 * Implementação real prevista para a Fase 4 (ROADMAP.md).
 */

/**
 * @param {object} _pad - objeto PAD completo (ver ARCHITECTURE.md §4)
 * @returns {{ titulo: string, secoes: Array<{ heading: string, conteudo: string }> }}
 */
export function renderizar(_pad) {
  throw new Error(
    'registroInfracaoTemplate.renderizar: implementação prevista para a Fase 4 (ver ROADMAP.md).',
  );
}
