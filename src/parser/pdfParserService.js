/**
 * Extração de texto/estrutura de PDF via PDF.js. Não implementado nesta
 * fase — ver README.md deste diretório e ROADMAP.md (Fase 3).
 */

/**
 * @param {File} _arquivoPdf
 * @returns {Promise<{ paginas: string[], textoCompleto: string }>}
 */
export async function extrairTexto(_arquivoPdf) {
  throw new Error('extrairTexto: implementação prevista para a Fase 3 (ver ROADMAP.md).');
}
