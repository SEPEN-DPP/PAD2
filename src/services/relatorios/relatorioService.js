/**
 * Serviço de relatórios gerenciais. Implementação real (agregações por
 * unidade/período/classificação) é da Fase 7 — ver ROADMAP.md. Esta fase
 * expõe apenas o contrato da função para a página já poder ser construída.
 */

/**
 * @param {object} _filtros
 * @returns {Promise<Array>}
 */
export async function gerarRelatorio(_filtros) {
  throw new Error('gerarRelatorio: implementação prevista para a Fase 7 (ver ROADMAP.md).');
}
